# Core dependencies
requests>=2.28.0
pyyaml>=6.0
markdown>=3.4.1

# Document parsing
PyMuPDF>=1.20.0     # for PDF parsing (fitz)
python-docx>=0.8.11 # for DOCX parsing

# Optional dependencies
# Uncomment if needed for your use case
# openai>=0.27.0    # for OpenAI API integration
# langchain>=0.0.200 # for more complex LLM workflows 